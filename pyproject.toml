[project]
name = "lasd"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
    "diffusers>=0.30",
    "transformers",
    "accelerate",
    "safetensors",
    "timm",
    "numpy",
    "scipy",
    "scikit-learn",
    "pillow",
    "tqdm",
]
